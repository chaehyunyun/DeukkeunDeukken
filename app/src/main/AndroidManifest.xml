<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylogin">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLogin">

        <activity android:name=".NewId"> </activity>
        <activity android:name=".FindPw"> </activity>
        <activity android:name=".AfterLogin"> </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".second_window">
        </activity>
        <activity android:name=".home"></activity>
        <activity
            android:name=".ex_detail"
            android:parentActivityName=".second_window">
        </activity>

        <activity android:name=".ex_list"></activity>

    </application>

</manifest>